
nullfxp 在 *nix平台安装步骤：
                                                
depens: 

1、 Qt 4 C++ Library 
2、 OpenSSL Library
3、 svn client

安装环境示例：
1、Qt 4 装在 /usr/ 目录下，那么qmake 在/usr/bin/目录下
2、OpenSSL 将在 /usr/ 目录下

安装：
bash> cd
bash> svn co https://nullfxp.svn.sourceforge.net/svnroot/nullfxp/trunk nullfxp-svn
...显示从svn下载下来的源码文件
bash> cd nullfxp-svn
bash> /usr/bin/qmake -v             ##确定你的Qt库版本,如下我版本如下，但至少需要Qt 4.0 或者以上版本
QMake version 2.01a
Using Qt version 4.3.1 in /usr/lib/qt4
bash> /usr/bin/qmake                #生成Makefile 文件
bash> make

如果以上依赖的包都没有问题，make命令会在nullfxp-svn/bin/目录下生成 nullfxp 可执行程序

bash> ./bin/nullfxp

可以开始使用了。

如果发现有什么问题，请发邮件到 liuguangzhao@users.sourceforge.net 寻求帮助。
或者也可以将您发现的本程序的 Bug 直接提交到此项目的 Bug 管理系统，地址是：http://www.qtchina.net/bug/

( 2007/09/02 )

